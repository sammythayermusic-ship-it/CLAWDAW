{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the CLAWDAW window — needs core IPC + the engine commands defined in engine.rs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
