# clawdaw-agent

MCP server that exposes the CLAWDAW audio engine to Claude as tools.

(See `docs/session-log.md` for the Phase 4 v1 build notes — this README is filled in once the server is working.)
